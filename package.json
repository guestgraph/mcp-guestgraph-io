{
  "name": "mcp-guestgraph-io",
  "version": "0.1.0",
  "private": true,
  "description": "mcp.guestgraph.io: GuestGraph's own model served over MCP",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "snapshot": "companygraph-mcp-deploy snapshot",
    "page-css": "companygraph-mcp-deploy page-css",
    "jsonld": "companygraph-mcp-deploy jsonld",
    "server-json": "companygraph-mcp-deploy server-json",
    "test": "node --test 'test/*.test.mjs'"
  },
  "dependencies": {
    "companygraph-mcp-server": "github:companygraph/mcp-server#v0.29.0"
  },
  "devDependencies": {
    "@modelcontextprotocol/client": "^2.1.0",
    "@robertblust/design": "github:robertblust/design#v0.84.2",
    "playwright": "^1.63.0"
  }
}
